spring:
  application.name: password-management
  cloud.config:
    uri: http://config-server:8888
    enabled: true
    fail-fast: true
    retry.max-attempts: 10
    username: ${USERNAME}
    password: ${PASSWD}
encrypt.key: ${key}
